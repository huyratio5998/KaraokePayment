@model IEnumerable<KaraokePayment.Data.Entity.NhanVienCaLV>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <div class="content-header">
        <div class="container-fluid">
            <!-- ds phòng -->
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <div class="row">
                                <div class="col-md-11">
                                    <h3 class="card-title" style="font-size:20px">Danh sach phong</h3>
                                </div>
                                <div class="col-md-1">
                                    <a asp-action="Create" class="btn btn-default">Thêm mới</a>
                                </div>
                            </div>
                        </div>
                        <!-- /.card-header -->
                        <div class="card-body">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.NgayLV)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.NhanVien)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.CaLv)
                                        </th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@Html.DisplayFor(x => item.Id)</td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.NgayLV)
                                            </td>
                                            <td>
                                                @{
                                                    var name = $"{item.NhanVien?.Ho} {item.NhanVien?.Ten}";
                                                    if (item.NhanVien == null && item.NhanVienId != null)
                                                    {
                                                        name = item.NhanVienId;
                                                    }
                                                }
                                                @name
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.CaLv.TenCa)
                                            </td>
                                            <td>
                                                <a class="btn btn-info btn-sm" asp-action="Edit" asp-route-id="@item.Id">
                                                    <i class="fas fa-pencil-alt">
                                                    </i>
                                                    Sửa
                                                </a>
                                                <a class="btn btn-primary btn-sm" asp-action="Details" asp-route-id="@item.Id">
                                                    <i class="fas fa-folder">
                                                    </i>
                                                    Chi tiết
                                                </a>
                                                <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@item.Id">
                                                    <i class="fas fa-trash">
                                                    </i>
                                                    Xóa
                                                </a>
                                            </td>
                                        </tr>
                                    }                                  
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>